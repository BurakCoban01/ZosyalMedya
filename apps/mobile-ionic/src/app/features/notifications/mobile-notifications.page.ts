import { DatePipe } from '@angular/common';import { ChangeDetectionStrategy,Component,OnDestroy,OnInit,signal } from '@angular/core';import { Router } from '@angular/router';import { Api,listNotifications,markNotificationRead,NotificationView } from '@platform/api';import { IonButton,IonContent,IonHeader,IonItem,IonLabel,IonList,IonNote,IonTitle,IonToolbar } from '@ionic/angular/standalone';import { MobileMessagingRealtimeService } from '../../core/realtime/mobile-messaging-realtime.service';
@Component({selector:'app-mobile-notifications',imports:[DatePipe,IonButton,IonContent,IonHeader,IonItem,IonLabel,IonList,IonNote,IonTitle,IonToolbar],template:`<ion-header translucent="true"><ion-toolbar><ion-title>Bildirimler</ion-title></ion-toolbar></ion-header><ion-content [fullscreen]="true"><div class="page-intro"><p>GÜNCEL</p><h1>Kaçırdıkların.</h1><span>Benzer olaylar tek bildirimde toplanır.</span></div>@if(message()){<ion-note class="status" role="status">{{message()}}</ion-note>}<ion-list inset="true">@for(item of items();track item.id){<ion-item button="true" detail="true" [disabled]="isPending(item.id)" [class.unread]="!item.isRead" (click)="open(item)"><ion-label class="ion-text-wrap"><h2>{{title(item)}} @if(item.count>1){<strong>· {{item.count}}</strong>}</h2><p>{{item.arguments['preview']||'Yeni bir gelişme var.'}}</p><p>{{item.createdAtUtc|date:'dd MMM · HH:mm'}}</p></ion-label></ion-item>}@empty{<ion-item><ion-label><h2>Her şey güncel.</h2><p>Yeni gelişmeler burada görünür.</p></ion-label></ion-item>}</ion-list>@if(nextCursor()){<ion-button expand="block" fill="outline" [disabled]="loadingMore()" (click)="load(true)">{{loadingMore()?'Yükleniyor…':'Daha fazla'}}</ion-button>}</ion-content>`,styles:[`.unread{--background:color-mix(in srgb,var(--ion-color-primary) 10%,var(--ion-item-background))}.unread h2{font-weight:800}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class MobileNotificationsPage implements OnInit,OnDestroy{readonly items=signal<NotificationView[]>([]);readonly message=signal('');readonly nextCursor=signal<string|null>(null);readonly loadingMore=signal(false);readonly pendingIds=signal(new Set<string>());private unsubscribe?:()=>void;private loadRevision=0;constructor(private readonly api:Api,private readonly realtime:MobileMessagingRealtimeService,private readonly router:Router){}ngOnInit():void{void this.load(false);this.unsubscribe=this.realtime.onNotification(()=>void this.load(false));void this.realtime.connect().catch(()=>undefined);}ngOnDestroy():void{this.unsubscribe?.();++this.loadRevision;}async load(append=false):Promise<void>{if(append&&(this.loadingMore()||!this.nextCursor()))return;const revision=append?this.loadRevision:++this.loadRevision;if(append)this.loadingMore.set(true);try{const page=await this.api.invoke(listNotifications,{limit:40,cursor:append?this.nextCursor()??undefined:undefined});if(revision!==this.loadRevision)return;this.items.set(append?this.mergeUnique(this.items(),page.items):page.items);this.nextCursor.set(page.nextCursor??null);}catch{if(revision===this.loadRevision)this.message.set('Bildirimler yüklenemedi. Mevcut listen korunuyor.');}finally{if(revision===this.loadRevision)this.loadingMore.set(false);}}async open(item:NotificationView):Promise<void>{if(this.isPending(item.id))return;this.setPending(item.id,true);this.message.set('');try{if(!item.isRead){const changed=await this.api.invoke(markNotificationRead,{notificationId:item.id});this.items.update(items=>items.map(current=>current.id===changed.id?changed:current));}const target=this.targetLink(item);if(target)await this.router.navigateByUrl(target);else this.message.set('Bildirim okundu; güvenli bir uygulama hedefi bulunamadı.');}catch{this.message.set('Bildirim açılamadı.');}finally{this.setPending(item.id,false);}}isPending(id:string):boolean{return this.pendingIds().has(id);}title(item:NotificationView):string{if(item.titleTemplateKey==='notification.follow_request.title')return'Yeni takip isteği';if(item.titleTemplateKey==='notification.question.title')return'Yeni soru';return item.type==='Message'?'Yeni mesaj':item.type==='Reaction'?'Yeni tepki':item.type==='Comment'?'Yeni yorum':item.type==='NewFollower'?'Yeni takipçi':'Yeni bildirim';}targetLink(item:NotificationView):string|null{if((item.type==='Reaction'||item.type==='Comment')&&item.entityId)return`/icerik/${item.entityId}`;if(item.titleTemplateKey==='notification.question.title'&&item.entityId)return`/sorular/${item.entityId}`;if(item.type==='NewFollower'&&item.arguments['followState']==='Pending')return'/baglantilar?view=requests';if(item.type==='NewFollower'&&item.arguments['actorHandle'])return`/profil/${encodeURIComponent(item.arguments['actorHandle'])}`;return this.isSafeInternalLink(item.deepLink)?item.deepLink:null;}private mergeUnique(current:NotificationView[],incoming:NotificationView[]):NotificationView[]{const byId=new Map(current.map(item=>[item.id,item]));for(const item of incoming)byId.set(item.id,item);return[...byId.values()];}private isSafeInternalLink(value:string):boolean{return value.startsWith('/')&&!value.startsWith('//')&&!value.includes('\\');}private setPending(id:string,pending:boolean):void{this.pendingIds.update(current=>{const next=new Set(current);if(pending)next.add(id);else next.delete(id);return next;});}}

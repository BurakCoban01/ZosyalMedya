import { ChangeDetectionStrategy,Component,OnDestroy,OnInit,signal } from '@angular/core';import { ActivatedRoute,RouterLink } from '@angular/router';import { Api,getQuestion,QuestionView } from '@platform/api';import { IonBackButton,IonButton,IonButtons,IonContent,IonHeader,IonTitle,IonToolbar } from '@ionic/angular/standalone';import { Subscription } from 'rxjs';
@Component({selector:'app-mobile-question-detail',imports:[RouterLink,IonBackButton,IonButton,IonButtons,IonContent,IonHeader,IonTitle,IonToolbar],template:`<ion-header translucent="true"><ion-toolbar><ion-buttons slot="start"><ion-back-button defaultHref="/sorular"></ion-back-button></ion-buttons><ion-title>Soru ve yanıt</ion-title></ion-toolbar></ion-header><ion-content [fullscreen]="true">@if(loading()){<section class="detail-state" aria-busy="true"><h1>Yanıt yükleniyor</h1></section>}@else if(error()){<section class="detail-state" role="alert"><h1>Soru açılamadı</h1><ion-button (click)="load()">Tekrar dene</ion-button></section>}@else if(question();as item){<article class="detail-card"><p class="kicker">SORU & YANIT</p><h1>{{item.isAnonymous?'Anonim bir soru':'Topluluktan bir soru'}}</h1>@if(item.isAnonymous){<p class="meta">Gönderen kimliği paylaşılmaz.</p>}@else if(item.sender){<a [routerLink]="['/profil',item.sender.handle]">{{item.sender.displayName}} · &#64;{{item.sender.handle}}</a>}<p>{{item.body}}</p><section class="answer"><h2>Yanıt</h2><p>{{item.answer}}</p></section></article>}</ion-content>`,styleUrl:'./mobile-detail.css',changeDetection:ChangeDetectionStrategy.OnPush})
export class MobileQuestionDetailPage implements OnInit,OnDestroy{readonly question=signal<QuestionView|null>(null);readonly loading=signal(true);readonly error=signal(false);private id='';private revision=0;private subscription?:Subscription;constructor(private readonly api:Api,private readonly route:ActivatedRoute){}ngOnInit():void{this.subscription=this.route.paramMap.subscribe(params=>{this.id=params.get('id')??'';const revision=++this.revision;this.question.set(null);void this.load(revision);});}ngOnDestroy():void{this.subscription?.unsubscribe();++this.revision;}async load(revision=this.revision):Promise<void>{this.loading.set(true);this.error.set(false);try{const question=await this.api.invoke(getQuestion,{id:this.id});if(revision===this.revision)this.question.set(question);}catch{if(revision===this.revision){this.question.set(null);this.error.set(true);}}finally{if(revision===this.revision)this.loading.set(false);}}}

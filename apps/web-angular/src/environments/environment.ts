export const environment = {
  production: false,
  // Dev: empty => same-origin relative URLs routed by apps/web-angular/proxy.conf.json
  // to the API on :58080. Matches prod's relative URLs; avoids CORS (AllowedOrigins is :58081 only).
  apiUrl: ''
};

/**
 * ZosyalMedya design-system — patterns barrel.
 *
 * Patterns are reusable compositions built from primitives + tokens that
 * encode a recurring product structure (a post frame, a reaction bar, a
 * composer, a thread, etc.). They are presentational; data + interaction
 * wiring lives in feature components.
 *
 *   import { ZmPostFrameComponent, ZmPostIdentityComponent } from
 *     '../design-system/patterns';
 */
export * from './post-frame.component';
export * from './post-identity.component';

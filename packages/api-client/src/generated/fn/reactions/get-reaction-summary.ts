/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReactionSummary } from '../../models/reaction-summary';

export interface GetReactionSummary$Params {
  contentId: string;
}

export function getReactionSummary(http: HttpClient, rootUrl: string, params: GetReactionSummary$Params, context?: HttpContext): Observable<StrictHttpResponse<ReactionSummary>> {
  const rb = new RequestBuilder(rootUrl, getReactionSummary.PATH, 'get');
  if (params) {
    rb.path('contentId', params.contentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReactionSummary>;
    })
  );
}

getReactionSummary.PATH = '/api/v1/content/{contentId}/reactions';

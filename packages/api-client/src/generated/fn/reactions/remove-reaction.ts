/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReactionView } from '../../models/reaction-view';

export interface RemoveReaction$Params {
  contentId: string;
}

export function removeReaction(http: HttpClient, rootUrl: string, params: RemoveReaction$Params, context?: HttpContext): Observable<StrictHttpResponse<ReactionView>> {
  const rb = new RequestBuilder(rootUrl, removeReaction.PATH, 'delete');
  if (params) {
    rb.path('contentId', params.contentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReactionView>;
    })
  );
}

removeReaction.PATH = '/api/v1/content/{contentId}/reaction';

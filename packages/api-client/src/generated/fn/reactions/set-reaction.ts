/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReactionView } from '../../models/reaction-view';
import { SetReactionRequest } from '../../models/set-reaction-request';

export interface SetReaction$Params {
  contentId: string;
      body: SetReactionRequest
}

export function setReaction(http: HttpClient, rootUrl: string, params: SetReaction$Params, context?: HttpContext): Observable<StrictHttpResponse<ReactionView>> {
  const rb = new RequestBuilder(rootUrl, setReaction.PATH, 'put');
  if (params) {
    rb.path('contentId', params.contentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReactionView>;
    })
  );
}

setReaction.PATH = '/api/v1/content/{contentId}/reaction';

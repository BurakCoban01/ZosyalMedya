/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionView } from '../../models/question-view';

export interface ArchiveQuestion$Params {
  id: string;
}

export function archiveQuestion(http: HttpClient, rootUrl: string, params: ArchiveQuestion$Params, context?: HttpContext): Observable<StrictHttpResponse<QuestionView>> {
  const rb = new RequestBuilder(rootUrl, archiveQuestion.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuestionView>;
    })
  );
}

archiveQuestion.PATH = '/api/v1/questions/{id}/archive';

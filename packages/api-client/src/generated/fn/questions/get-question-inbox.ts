/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionView } from '../../models/question-view';

export interface GetQuestionInbox$Params {
  status?: 'Draft' | 'Scheduled' | 'Published' | 'Answered' | 'Archived' | 'Deleted';
  limit?: number;
}

export function getQuestionInbox(http: HttpClient, rootUrl: string, params?: GetQuestionInbox$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QuestionView>>> {
  const rb = new RequestBuilder(rootUrl, getQuestionInbox.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QuestionView>>;
    })
  );
}

getQuestionInbox.PATH = '/api/v1/questions/inbox';

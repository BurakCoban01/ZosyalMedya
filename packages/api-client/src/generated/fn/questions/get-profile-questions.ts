/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionView } from '../../models/question-view';

export interface GetProfileQuestions$Params {
  targetId: string;
  limit?: number;
}

export function getProfileQuestions(http: HttpClient, rootUrl: string, params: GetProfileQuestions$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QuestionView>>> {
  const rb = new RequestBuilder(rootUrl, getProfileQuestions.PATH, 'get');
  if (params) {
    rb.path('targetId', params.targetId, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QuestionView>>;
    })
  );
}

getProfileQuestions.PATH = '/api/v1/questions/by-profile/{targetId}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnswerQuestionRequest } from '../../models/answer-question-request';
import { QuestionView } from '../../models/question-view';

export interface AnswerQuestion$Params {
  id: string;
      body: AnswerQuestionRequest
}

export function answerQuestion(http: HttpClient, rootUrl: string, params: AnswerQuestion$Params, context?: HttpContext): Observable<StrictHttpResponse<QuestionView>> {
  const rb = new RequestBuilder(rootUrl, answerQuestion.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuestionView>;
    })
  );
}

answerQuestion.PATH = '/api/v1/questions/{id}/answer';

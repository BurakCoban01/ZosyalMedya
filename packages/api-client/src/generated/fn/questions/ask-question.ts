/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AskQuestionRequest } from '../../models/ask-question-request';
import { QuestionView } from '../../models/question-view';

export interface AskQuestion$Params {
      body: AskQuestionRequest
}

export function askQuestion(http: HttpClient, rootUrl: string, params: AskQuestion$Params, context?: HttpContext): Observable<StrictHttpResponse<QuestionView>> {
  const rb = new RequestBuilder(rootUrl, askQuestion.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<QuestionView>;
    })
  );
}

askQuestion.PATH = '/api/v1/questions';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TrendingTagView } from '../../models/trending-tag-view';

export interface Trending$Params {
  limit?: number;
}

export function trending(http: HttpClient, rootUrl: string, params?: Trending$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TrendingTagView>>> {
  const rb = new RequestBuilder(rootUrl, trending.PATH, 'get');
  if (params) {
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TrendingTagView>>;
    })
  );
}

trending.PATH = '/api/v1/search/trending';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateModerationReportRequest } from '../../models/create-moderation-report-request';
import { ModerationReportView } from '../../models/moderation-report-view';

export interface CreateModerationReport$Params {
      body: CreateModerationReportRequest
}

export function createModerationReport(http: HttpClient, rootUrl: string, params: CreateModerationReport$Params, context?: HttpContext): Observable<StrictHttpResponse<ModerationReportView>> {
  const rb = new RequestBuilder(rootUrl, createModerationReport.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ModerationReportView>;
    })
  );
}

createModerationReport.PATH = '/api/v1/moderation/reports';

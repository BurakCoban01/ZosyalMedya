/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ModerationCaseView } from '../../models/moderation-case-view';

export interface ListModerationCases$Params {
  status?: string;
  limit?: number;
}

export function listModerationCases(http: HttpClient, rootUrl: string, params?: ListModerationCases$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ModerationCaseView>>> {
  const rb = new RequestBuilder(rootUrl, listModerationCases.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ModerationCaseView>>;
    })
  );
}

listModerationCases.PATH = '/api/v1/moderation/cases';

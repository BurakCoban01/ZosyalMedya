/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SavedContentPage } from '../../models/saved-content-page';

export interface ListSavedContent$Params {
  collection?: string;
  limit?: number;
  cursor?: string;
}

export function listSavedContent(http: HttpClient, rootUrl: string, params?: ListSavedContent$Params, context?: HttpContext): Observable<StrictHttpResponse<SavedContentPage>> {
  const rb = new RequestBuilder(rootUrl, listSavedContent.PATH, 'get');
  if (params) {
    rb.query('collection', params.collection, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SavedContentPage>;
    })
  );
}

listSavedContent.PATH = '/api/v1/content/saved';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PollView } from '../../models/poll-view';

export interface GetPoll$Params {
  contentId: string;
}

export function getPoll(http: HttpClient, rootUrl: string, params: GetPoll$Params, context?: HttpContext): Observable<StrictHttpResponse<PollView>> {
  const rb = new RequestBuilder(rootUrl, getPoll.PATH, 'get');
  if (params) {
    rb.path('contentId', params.contentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PollView>;
    })
  );
}

getPoll.PATH = '/api/v1/content/{contentId}/poll';

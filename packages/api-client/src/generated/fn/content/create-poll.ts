/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreatePollRequest } from '../../models/create-poll-request';
import { PollView } from '../../models/poll-view';

export interface CreatePoll$Params {
  contentId: string;
      body: CreatePollRequest
}

export function createPoll(http: HttpClient, rootUrl: string, params: CreatePoll$Params, context?: HttpContext): Observable<StrictHttpResponse<PollView>> {
  const rb = new RequestBuilder(rootUrl, createPoll.PATH, 'post');
  if (params) {
    rb.path('contentId', params.contentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PollView>;
    })
  );
}

createPoll.PATH = '/api/v1/content/{contentId}/poll';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ImpressionResult } from '../../models/impression-result';

export interface RecordImpression$Params {
  contentId: string;
  'X-View-Session'?: string;
}

export function recordImpression(http: HttpClient, rootUrl: string, params: RecordImpression$Params, context?: HttpContext): Observable<StrictHttpResponse<ImpressionResult>> {
  const rb = new RequestBuilder(rootUrl, recordImpression.PATH, 'post');
  if (params) {
    rb.path('contentId', params.contentId, {});
    rb.header('X-View-Session', params['X-View-Session'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ImpressionResult>;
    })
  );
}

recordImpression.PATH = '/api/v1/content/{contentId}/impressions';

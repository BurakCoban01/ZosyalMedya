/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PollView } from '../../models/poll-view';
import { VotePollRequest } from '../../models/vote-poll-request';

export interface VotePoll$Params {
  contentId: string;
      body: VotePollRequest
}

export function votePoll(http: HttpClient, rootUrl: string, params: VotePoll$Params, context?: HttpContext): Observable<StrictHttpResponse<PollView>> {
  const rb = new RequestBuilder(rootUrl, votePoll.PATH, 'post');
  if (params) {
    rb.path('contentId', params.contentId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PollView>;
    })
  );
}

votePoll.PATH = '/api/v1/content/{contentId}/poll/votes';

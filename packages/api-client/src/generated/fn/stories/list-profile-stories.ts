/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StoryPage } from '../../models/story-page';

export interface ListProfileStories$Params {
  ownerId: string;
  limit?: number;
  cursor?: string;
}

export function listProfileStories(http: HttpClient, rootUrl: string, params: ListProfileStories$Params, context?: HttpContext): Observable<StrictHttpResponse<StoryPage>> {
  const rb = new RequestBuilder(rootUrl, listProfileStories.PATH, 'get');
  if (params) {
    rb.path('ownerId', params.ownerId, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StoryPage>;
    })
  );
}

listProfileStories.PATH = '/api/v1/stories/profile/{ownerId}';

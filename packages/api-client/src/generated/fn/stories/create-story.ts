/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateStoryRequest } from '../../models/create-story-request';
import { StoryView } from '../../models/story-view';

export interface CreateStory$Params {
      body: CreateStoryRequest
}

export function createStory(http: HttpClient, rootUrl: string, params: CreateStory$Params, context?: HttpContext): Observable<StrictHttpResponse<StoryView>> {
  const rb = new RequestBuilder(rootUrl, createStory.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StoryView>;
    })
  );
}

createStory.PATH = '/api/v1/stories/';

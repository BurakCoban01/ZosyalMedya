/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ProfileView } from '../../models/profile-view';

export interface GetProfileByHandle$Params {
  handle: string;
}

export function getProfileByHandle(http: HttpClient, rootUrl: string, params: GetProfileByHandle$Params, context?: HttpContext): Observable<StrictHttpResponse<ProfileView>> {
  const rb = new RequestBuilder(rootUrl, getProfileByHandle.PATH, 'get');
  if (params) {
    rb.path('handle', params.handle, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ProfileView>;
    })
  );
}

getProfileByHandle.PATH = '/api/v1/profiles/{handle}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ChangeMessageRequest } from '../../models/change-message-request';
import { MessageView } from '../../models/message-view';

export interface ChangeMessage$Params {
  messageId: string;
      body: ChangeMessageRequest
}

export function changeMessage(http: HttpClient, rootUrl: string, params: ChangeMessage$Params, context?: HttpContext): Observable<StrictHttpResponse<MessageView>> {
  const rb = new RequestBuilder(rootUrl, changeMessage.PATH, 'patch');
  if (params) {
    rb.path('messageId', params.messageId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MessageView>;
    })
  );
}

changeMessage.PATH = '/api/v1/messaging/messages/{messageId}';

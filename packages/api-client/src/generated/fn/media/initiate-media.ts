/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InitiatedMedia } from '../../models/initiated-media';
import { InitiateMediaRequest } from '../../models/initiate-media-request';

export interface InitiateMedia$Params {
      body: InitiateMediaRequest
}

export function initiateMedia(http: HttpClient, rootUrl: string, params: InitiateMedia$Params, context?: HttpContext): Observable<StrictHttpResponse<InitiatedMedia>> {
  const rb = new RequestBuilder(rootUrl, initiateMedia.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<InitiatedMedia>;
    })
  );
}

initiateMedia.PATH = '/api/v1/media/';

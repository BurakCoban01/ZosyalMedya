/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MediaView } from '../../models/media-view';

export interface UploadMediaContent$Params {
  id: string;
      body: Blob
}

export function uploadMediaContent(http: HttpClient, rootUrl: string, params: UploadMediaContent$Params, context?: HttpContext): Observable<StrictHttpResponse<MediaView>> {
  const rb = new RequestBuilder(rootUrl, uploadMediaContent.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/octet-stream');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MediaView>;
    })
  );
}

uploadMediaContent.PATH = '/api/v1/media/{id}/content';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { IdentityDataExport } from '../../models/identity-data-export';

export interface ExportMyIdentityData$Params {
}

export function exportMyIdentityData(http: HttpClient, rootUrl: string, params?: ExportMyIdentityData$Params, context?: HttpContext): Observable<StrictHttpResponse<IdentityDataExport>> {
  const rb = new RequestBuilder(rootUrl, exportMyIdentityData.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<IdentityDataExport>;
    })
  );
}

exportMyIdentityData.PATH = '/api/v1/identity/privacy/export';

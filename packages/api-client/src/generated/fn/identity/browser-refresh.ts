/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BrowserAccessToken } from '../../models/browser-access-token';

export interface BrowserRefresh$Params {
  'X-CSRF-Token': string;
}

export function browserRefresh(http: HttpClient, rootUrl: string, params: BrowserRefresh$Params, context?: HttpContext): Observable<StrictHttpResponse<BrowserAccessToken>> {
  const rb = new RequestBuilder(rootUrl, browserRefresh.PATH, 'post');
  if (params) {
    rb.header('X-CSRF-Token', params['X-CSRF-Token'], {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BrowserAccessToken>;
    })
  );
}

browserRefresh.PATH = '/api/v1/identity/browser/refresh';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommunityView } from '../../models/community-view';

export interface SetCommunityPin$Params {
  id: string;
  contentId: string;
  pinned?: boolean;
}

export function setCommunityPin(http: HttpClient, rootUrl: string, params: SetCommunityPin$Params, context?: HttpContext): Observable<StrictHttpResponse<CommunityView>> {
  const rb = new RequestBuilder(rootUrl, setCommunityPin.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.path('contentId', params.contentId, {});
    rb.query('pinned', params.pinned, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CommunityView>;
    })
  );
}

setCommunityPin.PATH = '/api/v1/communities/{id}/pins/{contentId}';

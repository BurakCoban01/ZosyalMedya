/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ChangeCommunityRequest } from '../../models/change-community-request';
import { CommunityView } from '../../models/community-view';

export interface ChangeCommunity$Params {
  id: string;
      body: ChangeCommunityRequest
}

export function changeCommunity(http: HttpClient, rootUrl: string, params: ChangeCommunity$Params, context?: HttpContext): Observable<StrictHttpResponse<CommunityView>> {
  const rb = new RequestBuilder(rootUrl, changeCommunity.PATH, 'post');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CommunityView>;
    })
  );
}

changeCommunity.PATH = '/api/v1/communities/{id}/change';

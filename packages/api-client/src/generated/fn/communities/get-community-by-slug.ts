/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommunityDetailView } from '../../models/community-detail-view';

export interface GetCommunityBySlug$Params {
  slug: string;
}

export function getCommunityBySlug(http: HttpClient, rootUrl: string, params: GetCommunityBySlug$Params, context?: HttpContext): Observable<StrictHttpResponse<CommunityDetailView>> {
  const rb = new RequestBuilder(rootUrl, getCommunityBySlug.PATH, 'get');
  if (params) {
    rb.path('slug', params.slug, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CommunityDetailView>;
    })
  );
}

getCommunityBySlug.PATH = '/api/v1/communities/{slug}';

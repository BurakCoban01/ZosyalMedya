/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CommunityMemberView } from '../../models/community-member-view';

export interface ListCommunityMembers$Params {
  id: string;
  status?: 'Pending' | 'Invited' | 'Active' | 'Removed';
  limit?: number;
}

export function listCommunityMembers(http: HttpClient, rootUrl: string, params: ListCommunityMembers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CommunityMemberView>>> {
  const rb = new RequestBuilder(rootUrl, listCommunityMembers.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('status', params.status, {});
    rb.query('limit', params.limit, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CommunityMemberView>>;
    })
  );
}

listCommunityMembers.PATH = '/api/v1/communities/{id}/members';

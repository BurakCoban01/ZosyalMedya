/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RelationshipView } from '../../models/relationship-view';

export interface GetRelationship$Params {
  targetId: string;
}

export function getRelationship(http: HttpClient, rootUrl: string, params: GetRelationship$Params, context?: HttpContext): Observable<StrictHttpResponse<RelationshipView>> {
  const rb = new RequestBuilder(rootUrl, getRelationship.PATH, 'get');
  if (params) {
    rb.path('targetId', params.targetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RelationshipView>;
    })
  );
}

getRelationship.PATH = '/api/v1/social-graph/{targetId}';

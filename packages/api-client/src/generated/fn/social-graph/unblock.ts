/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RelationshipView } from '../../models/relationship-view';

export interface Unblock$Params {
  targetId: string;
}

export function unblock(http: HttpClient, rootUrl: string, params: Unblock$Params, context?: HttpContext): Observable<StrictHttpResponse<RelationshipView>> {
  const rb = new RequestBuilder(rootUrl, unblock.PATH, 'delete');
  if (params) {
    rb.path('targetId', params.targetId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RelationshipView>;
    })
  );
}

unblock.PATH = '/api/v1/social-graph/{targetId}/block';

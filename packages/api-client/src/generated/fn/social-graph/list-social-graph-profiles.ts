/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { SocialGraphPage } from '../../models/social-graph-page';

export interface ListSocialGraphProfiles$Params {
  ownerId: string;
  kind: 'Followers' | 'Following';
  limit?: number;
  cursor?: string;
}

export function listSocialGraphProfiles(http: HttpClient, rootUrl: string, params: ListSocialGraphProfiles$Params, context?: HttpContext): Observable<StrictHttpResponse<SocialGraphPage>> {
  const rb = new RequestBuilder(rootUrl, listSocialGraphProfiles.PATH, 'get');
  if (params) {
    rb.path('ownerId', params.ownerId, {});
    rb.path('kind', params.kind, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SocialGraphPage>;
    })
  );
}

listSocialGraphProfiles.PATH = '/api/v1/social-graph/{ownerId}/profiles/{kind}';

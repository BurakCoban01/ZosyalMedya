/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FeatureFlagView } from '../../models/feature-flag-view';
import { SetFeatureFlagRequest } from '../../models/set-feature-flag-request';

export interface SetFeatureFlag$Params {
  key: string;
      body: SetFeatureFlagRequest
}

export function setFeatureFlag(http: HttpClient, rootUrl: string, params: SetFeatureFlag$Params, context?: HttpContext): Observable<StrictHttpResponse<FeatureFlagView>> {
  const rb = new RequestBuilder(rootUrl, setFeatureFlag.PATH, 'put');
  if (params) {
    rb.path('key', params.key, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FeatureFlagView>;
    })
  );
}

setFeatureFlag.PATH = '/api/v1/administration/configuration/flags/{key}';

/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FeedPage } from '../../models/feed-page';

export interface GetFeed$Params {
  kind: 'Following' | 'Discovery' | 'Profile';
  limit?: number;
  cursor?: string;
  profileId?: string;
}

export function getFeed(http: HttpClient, rootUrl: string, params: GetFeed$Params, context?: HttpContext): Observable<StrictHttpResponse<FeedPage>> {
  const rb = new RequestBuilder(rootUrl, getFeed.PATH, 'get');
  if (params) {
    rb.path('kind', params.kind, {});
    rb.query('limit', params.limit, {});
    rb.query('cursor', params.cursor, {});
    rb.query('profileId', params.profileId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FeedPage>;
    })
  );
}

getFeed.PATH = '/api/v1/feed/{kind}';
